<h2>Upload a Player Picture</h2>

<%= error_messages_for :avatar %>

<% form_for(:avatar, :url=>avatars_path, :html=>{:multipart=>true}) do |f| %>
  <p>File:<%= f.file_field :uploaded_data %></p>
  <p><%= submit_tag 'Create' %></p>
<% end %>